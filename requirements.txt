PySide6>=6.7
pydantic[email]>=2.7
jinja2>=3.1
python-dateutil>=2.9

# Génération PDF (HTML → PDF)
weasyprint>=62.0
pdfkit>=1.0.0

# Google API (Agenda + OAuth2)
google-api-python-client>=2.147.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0

# Export Agenda ICS
ics>=0.7

# (optionnel) pour compatibilité PDF alternative
reportlab>=4.2
